from aiogram import types
from aiogram.dispatcher.filters import Text

from loader import dp

# @dp.message_handler(Text(equals = ["Product2", "Показать товар", "Product"]))
# async def text_product(message: types.Message):
# 	print('text_product -> message.text={}'.format(message.text))
